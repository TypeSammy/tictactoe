// Assigning X and O to player, and any other basic variables that might be needed

let playerOne = "X"
let playerTwo = "O"
let playersTurn = playerOne

const allCellsElement = document.querySelectorAll(".cell")

function alternatePlayer() {
  //
}


function cellClick() {
  // 
}
